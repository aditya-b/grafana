import { getFlotPairs } from '@grafana/ui';

describe('mupppo', () => {
  it('should work', () => {
    expect(true).toEqual(true);
  });
  it('should work3', () => {
    expect(getFlotPairs).toEqual(false);
  });
});
